class product {
  String? name;
  double price;
  String? desc;
  String? image;
  bool isSelected;

  product(
      {required this.name,
      required this.price,
      required this.image,
      required this.desc,
      required this.isSelected});
}
